from enum import Enum


class ToolName(Enum):
    SEARCH = "search"
    SPEAK = "speak"
    WOLFRAM_APLHA = "wolfram-alpha"
    HUMAN = "human"
    COMPANY_DIRECTORY = "company-directory"
    WAIT = "wait"
